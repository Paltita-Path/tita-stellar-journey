# Environment example for Vercel/GitHub export
# NOTE: Lovable does not use .env at runtime. Use Supabase Secrets for server-side keys.

# Supabase (public)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# OpenAI (server-side only, store in Supabase Secrets)
OPENAI_API_KEY=

# Stellar (server-side only, store in Supabase Secrets)
STELLAR_ISSUING_SECRET=
STELLAR_ISSUING_PUBLIC_KEY=
